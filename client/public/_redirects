# Netlify redirects and rewrites file.
# Replace the URLs below with your actual Render backend URL:
/api/*  https://togbcm.onrender.com/api/:splat  200
/uploads/*  https://togbcm.onrender.com/uploads/:splat  200

# React Router / SPA fallback rule (must be at the bottom)
/*  /index.html  200
